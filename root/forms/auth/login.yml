---
# action: /login
indicator: submit

constraints:
  - SingleValue

elements:
  - type: Text
    label_loc: ui.form.label.login
    name: username
    constraints:
        - Required

  - type: Password
    name: password
    label_loc: ui.form.label.password
    constraints:
        - Required

  - type: Submit
    name: submit

